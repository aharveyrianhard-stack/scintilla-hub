# PUBLISHER REVIEW — R7

**THE SCINTILLA TRADING RULEBOOK · Publisher ↔ Author · Return-review of Sprint 7 ("The Context Pair")**
**Reviewed:** `drafts/13-REGIME.md` (9 cards, v1), `drafts/14-MTF.md` (8 cards, v1), plus Publisher Note R6.1 applied across the early chapters (attribution strip).
**Form:** VERDICT · CORRECTIONS · RULINGS · STRENGTHS ON RECORD · GO-AHEAD.

---

## 1. VERDICT

**ACCEPTED, no send-backs.** The keystone is placed. Regime (Chapter 13) turns the signal into a decision, and it does it the only legal way — by making context gate *activity* while truth stays untouchable. MTF (Chapter 14) is its other half: which frame's facts a decision governs from. Together they cash the empirical spine the loop handed us six sprints early: the chain has no unconditional edge, so the book now conditions on a stated regime everywhere, and "trade with the higher timeframe" finally has a column instead of a slogan. Six open questions ruled, one light verification, then Sprint 8 — which finishes the draft.

---

## 2. CORRECTIONS

**None to the cards.** One verification: Note R6.1 was applied (Vocabulary and the early chapters were touched), so confirm on your next pass that **no residual "Alan amendment" or personal attribution survives anywhere** (Indicators I-2/I-8 especially) — the law reads as the "no-stacking / independence law" grounded in statistical hygiene, full stop. If any fingerprint remains, strip it.

---

## 3. RULINGS (six open questions)

**Regime:**
- **X-R1 · TREND-IN-RANGE counter-trend traps: MEASURE, don't gate.** Keep springs/upthrusts against the trend ACTIVE with the state stamp; gate only if the state's trap odds come back inverted. Your call — the table decides, not a prior.
- **X-R2 · dead-zone scope (uniform after every transition): AFFIRM uniform for v1.** Two-strangers simplicity wins now; the loop flags if range-birth dead zones cost more edge than they save. Measure the exception before carving it.
- **X-R3 · `regime.lookup` as a swept object: CONFIRMED, and tightened.** Cells change only on outcome evidence that clears the loop's own gates, never a single green run — yes. **Added architect condition:** the lookup's cells are *trials*, so the loop must count them in the deflated-Sharpe trial total and run the family-SPA over the swept table — sweeping a grid of cells is exactly the multiple-comparisons surface G5/G6 exist to police. Powerful *and* dangerous, handled by the same honesty as every other sweep. (Routed to the loop in the next architect review.)

**MTF:**
- **M-R1 · v1 stack {W CONTEXT, D RECORD}: HOLD at two frames.** Every added frame doubles the conflict surface and there's no measured alignment lift yet. Monthly joins when the alignment tables justify a third — not before. Your lean, ratified.
- **M-R2 · FRAME-FIRST default: CONFIRMED (the D-8 pattern).** Your ground is sound — frame authority is bar-contract authority (a weekly close is settled truth at scale), degree is recursion over one frame. It ships as the stated default and the loop can flip it per class on evidence.
- **M-R3 · WARN scope (confirmed-only): AFFIRM for v1, claim-based pre-authorized for wave 2.** Confirmed-only keeps the warning honest now; the lead-time table (`mtf.warnOdds`) shows whether an earlier, claim-based WARN is worth its false-positive rate. If it is, admit it then.

---

## 4. STRENGTHS ON RECORD

- **X-4's falsifiability clause is the soul of the whole book, in one sentence:** "a regime that could protect its own verdicts from disproof would be a religion with a lookup table." Context suspends *activity* — it may never touch a void path, an in-flight claim, or a verdict's truth. That single discipline is what separates this chapter from every discretionary "trade the regime" system ever written.
- **X-2, the four-state taxonomy from ledgers you already stamp.** No new detector, no new judgment — TRENDING / RANGING / TREND-IN-RANGE / TRANSITIONAL is arithmetic on S-4, R-objects, and S-5. "A regime scheme with nine moods is a discretionary system with extra paperwork" — exactly right, and TREND-IN-RANGE (the flag the chart draws, read as a pair of ledgers) is the sharp addition.
- **X-9's stated-regime law makes the machine's finding permanent furniture.** The loop discovered its own keystone (no pooled edge) before this chapter existed; X-9 writes that into the book as a promotion precondition and a column on every honesty table. The two desks closed the loop on the single most important empirical result of the project.
- **X-8's earnings split.** Stand the volume down, read the price, split the tables — the one bar the field argues most about, and each of its two faces gets exactly one treatment. Clean.
- **M-7's mixing table makes the upward ban *auditable*.** Keying every cross-frame ledger by reading-frame means daily touches physically cannot pad a weekly level's touch count — "invisible in prose, obvious in a ledger key." And M-8 sends the book's *own* FRAME-FIRST deference to the UNMEASURED table: if the alignment column ever reads flat, top-down doctrine demotes like anything else. The book holds itself to its own standard.

---

## 5. GO-AHEAD — SPRINT 8 ("Finish the Draft")

Eleven chapters plus the two keystones are done. Three remain — and one of them is light — so Sprint 8 is the one that completes the full first draft. Write all three:

1. **CH 12 — PATTERNS (P).** The composition chapter — cheap by design, and say so. The rectangle is already a mature range (R-9 alias); M/W tops and bottoms are EQ-runs (S-2) + divergence (I-9); flags and pennants are counter-trend channels (D-5/D-7); H&S necklines are diagonals (D-1 with the role-slope override). Per pattern: the definition sequence *by citation to existing objects*, the confirmation trigger (a B-1 chain — patterns inherit the break machinery and regime treatment through the chain, so this chapter never needs to know a pattern's name downstream), INVALIDATION, measured objective (B-6 grammar), the volume signature (Q columns), and the honest failure/busted-pattern statistics (the Bulkowski tables, loop-filled). No new machinery — this chapter is almost entirely aliases and honesty columns.
2. **CH 15 — UNIVERSE & RELATIVITY (U).** The application layer. Instrument classes as profiles; the index problem (adaptive/percentile thresholds — §J cashed, "the S&P's RSI never reaches 30"); the normalization law (volatility units, never raw price, so rules transfer); relative strength as a ratio series that gets the book's full treatment; macro-vs-ticker authority; and the intraday/session build-out every chapter has been depositing to (per-class sessions, ENTRY-frame activation per M-6, `vol.todSessions`, per-class calendars and stacks).
3. **CH 16 — RISK PROFILE & APPLICATION (K).** The closing chapter. CONSERVATIVE/MODERATE/AGGRESSIVE as parameter sets; the invariance law (definitions, confirmations, invalidations NEVER vary by profile — appetite changes exposure, never truth); the legal knobs (entry doctrine choice, stop distances, sizing, gated setup tiers); profile × regime interactions (regime/climate/HTF-alignment size through the legal channel only); mechanical de-risking after drawdown; one card per rule, profile-keyed — no forked rulebooks.

If Patterns' lightness lets all three land at depth, the draft is *complete* this sprint. If Risk needs more room, spill it into a short Sprint 9 alongside the formalization/appendix pass (glossary, master rule index, the Graveyard, the consolidated parameter registry, the changelog). Either way we're one, at most two, sprints from a finished book.

Deliver three lanes, five-part return. Then the book is done, and the loop tests it end to end.

*Publisher Review R7 · accepted · 0 corrections, 6 rulings · — THE PUBLISHER (Alan + orchestrating session)*
